
import Educations from '../../educations/Educations'
import './timelineCard.css'


function TimelineCard() {


    return (
        <>
        </>
    )
}

export default TimelineCard

            {/* {
                items.map((item) => (
                    <div className="timelineCard" key={item.id}>
                        <div className='timelineCard__info-wrapper'>
                            <h3 className='timelineCard__title'>{item.title}</h3>
                            <p className='timelineCard__year'>{item.yearFrom} - {item.yearTo}</p>
                            {item.company && <p className='timelineCard__company'>Företag: {item.company}</p>}
                            {item.institution && <p className='timelineCard__education'>Skola: {item.institution}</p>}
                            <p className='timelineCard__description'>{item.description}</p>
                        </div>
                    </div>
                ))
            } */}